# robots.txt for heyflou.com
#
# One group only. A crawler obeys the single most-specific matching group,
# so per-bot "Allow: /" groups made the Disallow lines below apply to nobody.
# Nothing is blocked by default, so search and AI crawlers are already allowed.

User-agent: *
Allow: /

# Private paths
Disallow: /admin/
Disallow: /dashboard/
Disallow: /api/

Sitemap: https://heyflou.com/sitemap.xml
